/**
 * @format
 */

import {AppRegistry} from 'react-native';
import PlayerExample from './src/PlayerExample';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => PlayerExample);
